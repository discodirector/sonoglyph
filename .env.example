# =============================================================================
# SONOGLYPH — environment template
# Скопируй в .env (cp .env.example .env) и заполни значения.
# .env уже в .gitignore — не коммитим.
# =============================================================================

# -----------------------------------------------------------------------------
# Hermes (Nous Research) — главный агент-проводник
# Получить: https://hermes-agent.nousresearch.com (или Nous API portal)
# -----------------------------------------------------------------------------
HERMES_API_KEY=
HERMES_API_BASE=https://inference-api.nousresearch.com/v1
HERMES_MODEL=Hermes-4-405B

# -----------------------------------------------------------------------------
# Kimi (Moonshot AI) — финальный полевой журнал из лога сессии (long context)
# Получить: https://platform.moonshot.ai/console/api-keys
# -----------------------------------------------------------------------------
KIMI_API_KEY=
KIMI_API_BASE=https://api.moonshot.ai/v1
KIMI_MODEL=kimi-k2-0905-preview

# -----------------------------------------------------------------------------
# ElevenLabs — TTS для голоса проводника
# Получить: https://elevenlabs.io/app/settings/api-keys
# VOICE_ID — выбрать на странице voice library мужской атмосферный голос,
# скопировать ID из URL или панели "Voice ID".
# Рекомендую: Adam, Daniel, или клонированный deep voice.
# -----------------------------------------------------------------------------
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=
ELEVENLABS_MODEL=eleven_turbo_v2_5

# -----------------------------------------------------------------------------
# IPFS pinning — для аудио-записи сессии и текста журнала Kimi
# Достаточно одного из двух (web3.storage предпочтительнее, есть free tier).
# -----------------------------------------------------------------------------
WEB3_STORAGE_TOKEN=
PINATA_JWT=

# -----------------------------------------------------------------------------
# Monad testnet — деплой контракта (день 5)
# Faucet: https://faucet.monad.xyz
# Docs: https://docs.monad.xyz
# -----------------------------------------------------------------------------
MONAD_RPC_URL=https://testnet-rpc.monad.xyz
MONAD_CHAIN_ID=10143
DEPLOYER_PRIVATE_KEY=
# Заполнится после деплоя, frontend читает это для mint:
SONOGLYPH_CONTRACT_ADDRESS=

# -----------------------------------------------------------------------------
# Прокси-сервер (локально)
# -----------------------------------------------------------------------------
PROXY_PORT=8787
PROXY_ORIGIN=http://localhost:5173

# -----------------------------------------------------------------------------
# Shared Hermes agents — для пользователей у которых нет своего Hermes.
# Бридж сам спавнит ephemeral hermes-процесс на VPS на 10 минут.
#
# HERMES_HOME_PATH    — глобальная папка Hermes на VPS (обычно /root/.hermes
#                       при запуске бриджа под root'ом). Оттуда копируется
#                       .env (API ключи) + config.yaml (модель/провайдер) в
#                       per-session HERMES_HOME=/tmp/sonoglyph-agents/<code>/.hermes
# HERMES_BIN_PATH     — имя/абсолютный путь к бинарю. Default 'hermes'
#                       полагается на $PATH из systemd EnvironmentFile.
# SHARED_AGENT_*      — лимиты. По умолчанию 5 одновременных, 250 в сутки
#                       (= max supply NFT), 10 мин hard timeout на процесс,
#                       1 спавн в минуту per IP.
# SHARED_AGENT_MODEL_OVERRIDE — опциональная замена model.default из глобального
#                       config.yaml. Оставь пустым чтобы использовать ту же
#                       модель что и в твоём собственном Hermes.
# -----------------------------------------------------------------------------
HERMES_HOME_PATH=/root/.hermes
HERMES_BIN_PATH=hermes
SHARED_AGENT_MAX_CONCURRENT=5
SHARED_AGENT_DAILY_CAP=250
SHARED_AGENT_TIMEOUT_MS=600000
SHARED_AGENT_RATE_LIMIT_MS=60000
SHARED_AGENT_RATE_LIMIT_MAX=1
SHARED_AGENT_MODEL_OVERRIDE=
